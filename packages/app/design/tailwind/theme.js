// @ts-check

/** @type {import('tailwindcss').Config['theme']} */
const theme = {
	// edit your tailwind theme here!
	// https://tailwindcss.com/docs/adding-custom-styles
	fontFamily: {
		primary: ["var(--font-playfair-display)"],
		secondary: ["var(--font-dm-sans)"],
	},
};

module.exports = {
	theme,
};
